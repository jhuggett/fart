// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as json$0 from "../encoding/json/models.js";

/**
 * Caps says what this machine can do with files, so the menus can say
 * "Move to Trash" or "Delete", "Reveal in Finder" or nothing.
 */
export interface Caps {
    /**
     * Remove moves to the Trash rather than deleting
     */
    "trash": boolean;

    /**
     * the file browser's name, "" if there is none to open
     */
    "reveal": string;
}

/**
 * ChatEvent is one thing the page shows: a line of text, a tool Claude
 * used, the end of a turn.
 */
export interface ChatEvent {
    /**
     * init | text | tool | result | error | done | log
     */
    "kind": string;
    "text"?: string;
    "name"?: string;
    "input"?: string;
    "session"?: string;
    "cost"?: number;
    "model"?: string;

    /**
     * "none" when a plan pays, else the API key's origin
     */
    "keySource"?: string;
}

/**
 * ChatInfo says whether Claude Code is on this machine, and whose it is:
 * the login (a claude.ai plan, or an API key) decides what a turn costs
 * the user, so the page can say so.
 */
export interface ChatInfo {
    "found": boolean;
    "path": string;
    "busy": boolean;
    "loggedIn": boolean;

    /**
     * "claude.ai" for a plan, else an API key or nothing
     */
    "authMethod": string;
    "email": string;

    /**
     * "max", "pro", … when the login is claude.ai
     */
    "plan": string;
    "org": string;
}

export interface ServeInfo {
    "on": boolean;
    "url": string;
    "root": string;

    /**
     * a PNG data URL of the URL, for the screen
     */
    "qr": string;
}

/**
 * Text is a file's content and whether there was one.
 */
export interface Text {
    "text": string;
    "found": boolean;
}

/**
 * ToolCall is what the page receives: a call to answer with ToolReply.
 */
export interface ToolCall {
    "id": string;
    "name": string;
    "args": json$0.RawMessage;
}
