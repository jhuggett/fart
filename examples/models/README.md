# examples/models

Models made in Uranus, by hand: a dog (`dog.fart`, six states from
`idle` to `sit`) and a flintlock with a pan flash (`flintlock.fart`,
from `half_cock` to `spent`). Both are 3D documents; open the folder in
Uranus for the model screen, or project them with `npx fart project`.
